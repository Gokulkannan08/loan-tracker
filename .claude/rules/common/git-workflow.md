# Git Workflow

This project is not a git repository. Git operations are not applicable.

If the project is initialized as a git repo in the future, use conventional commits:
```
<type>: <description>
```
Types: `feat`, `fix`, `refactor`, `docs`, `chore`, `perf`
